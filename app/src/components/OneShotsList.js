import React, { Component } from 'react';
import { firebaseApp } from '../../firebase';

class OneShotsList extends Component {
  render(){
    return(
      <div>
        <h4>One Shots List</h4>
      </div>
    );
  }
}

export default OneShotsList;
